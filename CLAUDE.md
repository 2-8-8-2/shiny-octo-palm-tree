# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is in its initial state. The only file present is a bare `README.md` with the project name (`shiny-octo-palm-tree`). No language, framework, build system, or tooling has been established yet.

When the project is bootstrapped, update this file with:
- Build, lint, and test commands (including how to run a single test)
- High-level architecture and module boundaries
- Any non-obvious conventions or constraints
